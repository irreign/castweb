{
  "name": "stylebook-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for the StyleBook AI hairstyle preview feature. Serves the static site and proxies photo edit requests to the OpenAI Images API so the API key never reaches the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.1.0"
  }
}
