// Production build configuration. See Dev.xcconfig for why the anon key
// is safe to embed here.
// See Dev.xcconfig for why the slashes are broken up with "$()".
SUPABASE_URL = https:/$()/YOUR-PROD-PROJECT.supabase.co
SUPABASE_ANON_KEY = replace-with-your-prod-anon-key
