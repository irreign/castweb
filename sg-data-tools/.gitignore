# Real credentials — never committed. Copy .env.example to .env and fill it in.
.env

# Pulled data and logs — local artifacts, not source. Reviewed and handed
# over by hand, not tracked in git.
*.output.json
*.output.*.json
run-log.txt
launchd-stdout.log
launchd-stderr.log
